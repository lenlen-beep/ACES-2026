[project]
name = "ACES-2026"
version = "0.1.0"
description = " "
authors = [
    { name=" ", email=" " }
]
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "numpy>=2.0",
    "pandas>=2.0",
    "geopandas>=1.0",
    "networkx>=3.0",
    "pyyaml>=6.0"
]

[project.optional-dependencies]
dev = []

[tool.setuptools.packages.find]
where = ["src"]